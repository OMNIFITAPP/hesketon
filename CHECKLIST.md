# רשימת פרסום — הסכתון

הזרימה האמיתית (מעודכן יולי 2026, אחרי כשל נאבל/לאזר): הכתיבה נעשית in-session
(קלוד הוא המנוע — אפס עלות API), והשערים המקומיים רצים בחינם עם node. שמור פתוח כשעובדים.

> הזרימה הישנה (content-queue.yml + GitHub Actions + Apify) עדיין קיימת בקוד,
> אבל דורשת קרדיט API — לא בשימוש כרגע.

---

## א. קליטת תמליל
- [ ] שמור את התמליל ב-`podcast-transcripts/to-do/` (סיומת `:classic` בשם = חובת האזנה).
- [ ] הרץ `npm run check:transcript -- "<קובץ>"`.
- [ ] 🔴 RAW_ASR? חובות נרמול לפני כתיבה: שחזור גבולות משפט; **כל שם פרטי חשוד כשיבוש ASR** — אימות מול מקור חיצוני; קריאת ההקשר המלא סביב כל מספר ויחידה (feet/מטרים!).
- [ ] אין תוויות דוברים בפרק מרובה-משתתפים? **תייגו דוברים לפני הכתיבה, לא תוך כדי.** שלושה סימנים: (א) גוף ראשון ברצף שנפתח בשאלת המנחה; (ב) צלע שלישית מדברת על האורח בגוף שלישי; (ג) **החלפת גוף בתוך רצף אחד = גבול שאלה/תשובה שה-ASR מיזג** ("...you felt that physically | with my hand yeah"). לא עבר את שלושתם → פרוזה, לא מרכאות.
- [ ] ציטוט שצד שלישי מוסר בשם האורח ("הוא אמר לי ש...") אינו ציטוט של האורח — פרוזה בייחוס למוסר.
- [ ] פרק מעל ~35K מילים: קבע מראש אילו 5–7 רעיונות נכנסים — לא לדחוס הכול.

## ב. כתיבה (in-session)
- [ ] עבוד לפי שלבי `scripts/lib/prompt.mjs` (אנליסט → כותב → קריטיק → עורך), עם `SYSTEM_PROMPT.md` כמקור-אמת.
- [ ] ציטוטים לפי חוזה הנאמנות (7 הכללים) — עיגון ורבטים בתמליל לפני תרגום; מקורות בבלוק `<!-- -->` בסוף הפוסט.
- [ ] בדיקה עצמית במצב צייד: מניחים שיש שגיאות ומחפשים אותן. בלי מחמאות ובלי ציונים — רק ממצאים.

## ג. שערים (חינם, לפני כל merge)
- [ ] `npm run check:reviews` — ירוק (אין פריטי סקירה פתוחים בפוסטים מפורסמים).
- [ ] `npm run check:units` — אין מיילים/פאונדים/רגליים. הקורא הישראלי לא חושב ביחידות אימפריאליות, וזה חל גם בתוך ציטוט.
- [ ] `npm run check-names` — שמות מול ויקיפדיה.
- [ ] `npm run check:refs` — אזכור מחקר ⇐ בלוק references, **ולכל רשומה `url`**. אזכור מחקר מחייב לינק. תפיסת-שווא ("מחקר של X" שאינו מחקר)? הצהר: `<!-- refs-keep: <נימוק> -->`.
- [ ] `npm run check:refs:verify` — מאמת מול doi.org שהלינק מצביע על **המאמר הנכון** (כתב עת + שנה), לא רק שהוא נפתח. הרץ לפני פרסום של פוסט עם הפניות חדשות.
- [ ] `npm run check:style` — מונחים שנפסלו במבחן המפגש + אחידות מינוח. מדווח גם על צפיפות מקפים (לא מפיל).
- [ ] **קריאה בקול של הליד והאמ;לק.** מכני לא תופס תרגומית. כל מילה שלא היית אומר לחבר — מוחלפת. ראה SYSTEM_PROMPT חוקים 12א ו-8.
- [ ] **בלוק המקורות רושם יחסים, לא רק אטומים.** כשפסקה מצמידה שני נתונים — מה היחס שהמקור טוען ביניהם? (SYSTEM_PROMPT חוק 9). רשימת עובדות מנותקות עיוורת בדיוק לכשל שהכתיבה עושה.
- [ ] **מספר אחד נושא-משקל לפסקה.** קרא הכול, פרסם מעט (חוק 3ב). מספר שלא עושה עבודה טיעונית — החוצה.
- [ ] `npm run build` — ירוק.
- [ ] ידני: `category` מאחת מ-6 הקטגוריות; `guestId`/`podcastId` קיימים ב-people.json/podcasts.json; `youtubeUrl` נכון; פרימיום? `premium: true` + 2–4 premiumHooks.

## ד. סקירה → יישום (התנאי ל-draft: false)
- [ ] קובץ סקירה נשמר בשם `reviews/<slug>-סקירה.md` — **בלי רווחים לפני הסיומת** (הרווח בשם קובץ נאבל הוא חלק ממה שהעלים את הסקירה).
- [ ] יישום בקומיט `polish(<slug>)`, ואז `npm run check:reviews` לאימות.
- [ ] **תיקון סגנון לעולם אינו נוגע בציטוט** (blockquote או מרכאות בפרוזה) בלי עיגון מחדש מול התמליל — הסקירה מצביעה, רק התמליל מכריע. (כך שרד "15 ס"מ" שגוי: הסקירה תיקנה לו רק את הדקדוק.)
- [ ] דחיית פריט סקירה במכוון? הצהר בפוסט: `<!-- review-keep: <קטע מהטקסט המקורי> + נימוק -->`.
- [ ] רק כשהכול ירוק — `draft: false`.

## ה. פרסום
- [ ] מזג את ה-PR ל-`main` → deploy אוטומטי; ודא ירוק ב-Actions.
- [ ] העבר את התמליל מ-to-do/ ל-done/.

## ו. הפצה (כשהניוזלטר פעיל)
- [ ] שלח גיליון (pull-quote + אמ;לק + לינק); סמן `inNewsletter: true`; שתף בערוצים.

---

### הפעלת רכיבים שממתינים
- **ניוזלטר:** הדבק את כתובת ה-POST של הספק ל-`NEWSLETTER.action` ב-`src/consts.ts` → הטופס מופיע לבד בפוטר ובכל פוסט.
- **חיפוש:** עובד אוטומטית בפרודקשן (Pagefind רץ ב-`postbuild`). מקומית: `npm run build && npm run preview`.
